// import { WebPlugin } from '@capacitor/core';
// import { ZipPlugin } from './definitions';

// export class ZipWeb extends WebPlugin implements ZipPlugin {
//   constructor() {
//     super({
//       name: 'Zip',
//       platforms: ['web'],
//     });
//   }

//   async zip(options: { src: string, dest: string }): Promise<any> {
//     return options;
//   }
//   async unzip(options: { src: string, dest: string }): Promise<any> {
//     return options;
//   }
// }

// const Zip = new ZipWeb();

// export { Zip };

// import { registerWebPlugin } from '@capacitor/core';
// registerWebPlugin(Zip);
